# Copy this file to .env and configure as needed

# Development mode - set to 'true' to enable filesystem endpoints in backend
# This allows the web app to read CSV files directly from the tracker's output
# Set to 'false' or omit for production deployment
DEV=true

# Backend server port (default: 9999)
PORT=9999

# Cookie encryption secret - GENERATE A NEW ONE FOR PRODUCTION!
# Use: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
COOKIE_SECRET=your-secure-random-key-here

# GitHub OAuth Integration (Optional - for commit history features)
# To set up:
# 1. Go to https://github.com/settings/developers
# 2. Click "OAuth Apps" → "New OAuth App"
# 3. Fill in:
#    - Application name: LogBridge
#    - Homepage URL: http://localhost:3000
#    - Authorization callback URL: http://localhost:3000/auth/callback
# 4. Copy the Client ID and generate a Client Secret
# 5. Add both values below
GITHUB_CLIENT_ID=your-github-client-id-here
GITHUB_CLIENT_SECRET=your-github-client-secret-here